class Solution {
public:
    vector<string> fizzBuzz(int n) {
            vector<string> res;
	for(int i = 1; i <= n; i++) 
        res.push_back(i % 5 ? (i % 3 ? to_string(i) : "Fizz") : (i % 3 ? "Buzz" : "FizzBuzz"));
	return res;
        }
        
    
};
