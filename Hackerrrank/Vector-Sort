#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int x;
    cin>>x;
    vector<int> y;
    while(cin>>x)
    {
        y.push_back(x);
    }
    sort(y.begin(),y.end());
    for(auto i=y.begin();i<y.end();i++)
    {
        cout<<*i<<" ";
    }
    return 0;
}
